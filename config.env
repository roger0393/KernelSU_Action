CONFIG_ENV=config.env

# ========== 源码 ==========
KERNEL_SOURCE=https://github.com/MiCode/Xiaomi_Kernel_OpenSource
KERNEL_SOURCE_BRANCH=peridot-u-oss

# ========== 内核配置 ==========
# Redmi Turbo 3 (peridot) 使用 GKI 基础配置 + 片段，KERNEL_CONFIG 填 gki_defconfig
# 实际编译时 build-kernel.yml 里会自动合并 peridot_GKI.config 和 peridot_consolidate.config
KERNEL_CONFIG=gki_defconfig

# ========== 编译产物 ==========
KERNEL_IMAGE_NAME=Image
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false

# ========== 额外编译命令 ==========
EXTRA_CMDS:LD=ld.lld

# ========== Clang ==========
## 使用自定义 Clang（这里保持默认不用自定义）
USE_CUSTOM_CLANG=false
CUSTOM_CLANG_SOURCE=
CUSTOM_CLANG_BRANCH=
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

## AOSP Clang（必须保留以下两行，否则编译会因找不到版本而失败）
CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r450784e

# ========== GCC（不需要可以都设为 false）==========
ENABLE_GCC_ARM64=false
ENABLE_GCC_ARM32=false
USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarch64-linux-android-
USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# ========== KernelSU ==========
ENABLE_KERNELSU=true
KERNELSU_TAG=v0.9.5
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# ========== 其他配置选项 ==========
DISABLE_LTO=false
DISABLE_CC_WERROR=false
ADD_KPROBES_CONFIG=false
ADD_OVERLAYFS_CONFIG=false
APPLY_KSU_PATCH=false
REMOVE_UNUSED_PACKAGES=true

# ========== AnyKernel3 ==========
USE_CUSTOM_ANYKERNEL3=false
CUSTOM_ANYKERNEL3_SOURCE=
CUSTOM_ANYKERNEL3_BRANCH=

# ========== Ccache ==========
ENABLE_CCACHE=true

# ========== DTBO ==========
NEED_DTBO=false

# ========== 构建 boot.img（不需要就保持 false）==========
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=https://raw.githubusercontent.com/xiaoleGun/KernelSU_action/main/boot/boot.img
